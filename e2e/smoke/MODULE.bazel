bazel_dep(name = "abc_dp_rules_cue", version = "0.0.0", dev_dependency = True)
bazel_dep(name = "bazel_skylib", version = "1.7.1", dev_dependency = True)

local_path_override(
    module_name = "abc_dp_rules_cue",
    path = "../..",
)
